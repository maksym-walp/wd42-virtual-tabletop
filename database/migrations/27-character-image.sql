-- ================================================================
-- Портрет персонажа. Та сама конвенція VARCHAR(500)-URL, що й у
-- каталожних таблиць: шлях /uploads/... або зовнішній https URL.
-- ================================================================

ALTER TABLE character_sheet.characters
  ADD COLUMN IF NOT EXISTS image_url VARCHAR(500);
